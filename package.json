{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "bootstrap-db": "node ./scripts/bootstrap-db.cjs",
    "env:api-dev": "./pnpm-env --filter @workspace/api-server run dev",
    "env:mobile-dev": "./pnpm-env --filter @workspace/mobile run dev",
    "env:db-push": "./pnpm-env --filter @workspace/db run push",
    "env:bootstrap-db": "./pnpm-env run bootstrap-db",
    "drop-db": "node ./scripts/drop-db.cjs"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "lightningcss-wasm": "^1.32.0"
  }
}
